{
  "name": "get-query",
  "version": "1.0.0",
  "description": "Get get request parameter from browser",
  "main": "dist/get-query.js",
  "scripts": {
    "build": "babel get-query.js --out-dir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sabbir Ahmed <mail@thesabbir.com>",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.5.2",
    "babel-preset-es2015": "^6.9.0"
  }
}
